/// Default durations
mixin Durations {
  /// Duration for sendTimeout
  static const Duration sendTimeout = Duration(seconds: 5);
  /// Duration for receiveTimeout
  static const Duration receiveTimeout = Duration(seconds: 5);
} 
